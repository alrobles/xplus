% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xplus.R
\name{xplus}
\alias{xplus}
\title{Fit an xplus model}
\usage{
xplus(
  x,
  y,
  alpha = 1,
  sample_use_time = 30,
  learning_rate = 1,
  qq = 0.1,
  verbose = FALSE,
  nfolds = 4,
  max_iter = 10000,
  convergence_threshold = 0.9,
  seed = NULL
)
}
\arguments{
\item{x}{Numeric feature matrix.}

\item{y}{Binary vector where \code{1} indicates known positives and \code{0} indicates unlabeled samples.}

\item{alpha}{Elastic-net mixing parameter passed to \code{\link[glmnet:cv.glmnet]{glmnet::cv.glmnet()}}.}

\item{sample_use_time}{Number of times each unlabeled sample can be selected before its sampling probability reaches zero.}

\item{learning_rate}{Learning-rate for pseudo-label updates in \verb{[0, 1]}.}

\item{qq}{Quantile used to define the positive-reference cutoff.}

\item{verbose}{Logical; print iterative progress messages.}

\item{nfolds}{Number of CV folds used in the iterative fit.}

\item{max_iter}{Maximum number of pseudo-labeling iterations.}

\item{convergence_threshold}{Proportion threshold for label stability and sampling exhaustion checks.}

\item{seed}{Integer seed passed to \code{\link[=set.seed]{set.seed()}} for reproducibility. When \code{NULL} (default), no seed is set.}
}
\value{
An object of class \code{"xplus"}.
}
\description{
Fit the PLUS algorithm for positive-unlabeled learning.
}
\details{
The PLUS algorithm alternates between fitting penalized logistic models on known positives
and sampled unlabeled cases, then updating unlabeled pseudo-labels from calibrated
probabilities. Convergence is reached when pseudo-label changes stabilize above
\code{convergence_threshold} or the sampling budget is consumed.
}
\examples{
\donttest{
x <- matrix(rnorm(200 * 10), ncol = 10)
y <- c(rep(1, 40), rep(0, 160))
fit <- xplus(x, y, max_iter = 20, seed = 1)
}
}
\references{
Zhou et al. (2022). doi:10.1371/journal.pcbi.1009956
}
\seealso{
\code{\link[=predict.xplus]{predict.xplus()}}, \code{\link[=summary.xplus]{summary.xplus()}}, \code{\link[=assess.xplus]{assess.xplus()}}
}
